% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crosstabs.R
\name{mfreq}
\alias{mfreq}
\title{Multi-variable frequency table (row or column percentages)}
\usage{
mfreq(data, vars = NULL, labels = FALSE, long = TRUE, stat = "p")
}
\arguments{
\item{data}{A data frame.}

\item{vars}{Character vector of variable names. If \code{NULL} (default), uses
every variable in \code{data} (only sensible if they share response levels).}

\item{labels}{Logical. If \code{TRUE}, use variable labels instead of names.
Default \code{FALSE}.}

\item{long}{Logical. If \code{TRUE} (default), one row per variable with
levels as columns. If \code{FALSE}, one column per variable with levels as
rows.}

\item{stat}{Which statistic to show: \code{"p"} (percentage, default) or
\code{"n"} (count).}
}
\value{
A tibble, shape depending on \code{long}, with a \code{Base} row/column
giving the number of non-missing cases per variable.
}
\description{
Tabulates several variables that share a common response scale (e.g. a
battery of trust/agreement items), returning either one row per variable
(\code{long = TRUE}) or one column per variable (\code{long = FALSE}).
}
