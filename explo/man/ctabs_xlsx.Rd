% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crosstabs.R
\name{ctabs_xlsx}
\alias{ctabs_xlsx}
\title{Export ctabs() output to a formatted Excel workbook}
\usage{
ctabs_xlsx(input, filename)
}
\arguments{
\item{input}{The list returned by \code{\link[=ctabs]{ctabs()}} (with \code{overview} and
\code{tables} elements).}

\item{filename}{Path to the \code{.xlsx} file to write.}
}
\value{
Invisibly, the path written to (via \code{\link[openxlsx:saveWorkbook]{openxlsx::saveWorkbook()}}).
}
\description{
Always writes an "Overview" worksheet first, with the marginal
frequencies for every variable involved (from \code{input$overview}) -- this
isn't optional/opt-in, since you should be able to see base rates
alongside the crosstabs by default. Then writes one worksheet per
outcome variable, stacking each predictor's crosstab below the previous
one with a merged predictor-label header row.
}
