% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/natural_breaks.R
\name{natural_breaks}
\alias{natural_breaks}
\title{Cut several numeric variables into a shared set of breaks}
\usage{
natural_breaks(
  data,
  vars,
  brk = NULL,
  nbrk = 10,
  stat = "p",
  p_na = FALSE,
  lab_style = 1
)
}
\arguments{
\item{data}{A data frame.}

\item{vars}{Character vector of numeric variable names to bin. All share
the same break points, so this works best when the variables have
similar ranges/scales.}

\item{brk}{Optional numeric vector of user-defined break points. If
supplied, \code{nbrk} is ignored and these breaks are used for every
variable in \code{vars}.}

\item{nbrk}{Number of breaks to generate automatically. Default \code{10}.}

\item{stat}{Which statistic(s) to return: \code{"p"} (percentage, default),
\code{"n"} (count), or \code{"b"} (both).}

\item{p_na}{Logical. If \code{FALSE} (default), percentages are recalculated
to exclude missing values. If \code{TRUE}, missing values are included in
the percentage base.}

\item{lab_style}{Integer \code{1} (default), \code{2}, or \code{3}, controlling how bin
labels are formatted (e.g. \code{"Greater than 0 and up to 10"} vs.
\code{"0 > & <= 10"} vs. \code{"0-10"}).}
}
\value{
A tibble with one row per bin and one column per variable
(or per variable/statistic combination if \code{stat = "b"}).
}
\description{
Uses \code{\link[=cut]{cut()}} to find a natural set of break points for \code{vars}, based on
the first variable, then widens the outer breaks so no value in any of
\code{vars} falls outside the range -- or applies a user-supplied set of
breaks (\code{brk}) directly.
}
