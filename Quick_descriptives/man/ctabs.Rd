% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crosstabs.R
\name{ctabs}
\alias{ctabs}
\title{Crosstab one or more outcomes against one or more predictors}
\usage{
ctabs(data, vars, preds)
}
\arguments{
\item{data}{A data frame.}

\item{vars}{Character vector of outcome variable names.}

\item{preds}{Character vector of predictor/crossbreak variable names.}
}
\value{
A nested list: one element per \code{vars} entry, each containing one
tibble per \code{preds} entry. Each tibble carries a \code{"tags"} attribute (see
\code{\link[=.add_tags]{.add_tags()}}) recording the predictor's variable label and the number
of predictor-level columns, used by \code{\link[=ctabs_xlsx]{ctabs_xlsx()}} for layout.
}
\description{
For each combination of \code{vars} (outcomes) and \code{preds} (predictors),
builds a percentage crosstab with a base row and a \code{total} column showing
the unconditional distribution of the outcome.
}
