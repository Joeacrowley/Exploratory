% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crosstabs.R
\name{ctab}
\alias{ctab}
\title{Two-way crosstab of one variable against a breakdown variable}
\usage{
ctab(
  data,
  vars = NULL,
  by,
  stat = "p",
  labels = TRUE,
  empty_levels = FALSE,
  drop_missing = "none"
)
}
\arguments{
\item{data}{A data frame.}

\item{vars}{Unquoted outcome variable to tabulate (data-masked, as in
\code{dplyr::count()}).}

\item{by}{Unquoted breakdown/crossbreak variable (data-masked).}

\item{stat}{Which statistic to show in the crossbreak columns: \code{"p"}
(percentage, default) or \code{"n"} (unweighted count).}

\item{labels}{Logical. If \code{TRUE} (default), rename the first column to
\code{vars}'s variable label.}

\item{empty_levels}{Logical. If \code{TRUE}, keep unused factor levels.
Default \code{FALSE}.}

\item{drop_missing}{Which missing data to drop before tabulating: \code{"none"}
(default), \code{"b"} (both \code{vars} and \code{by}), \code{"p"} (predictor/\code{by} only), or
\code{"o"} (outcome/\code{vars} only).}
}
\value{
A tibble with one row per level of \code{vars}, one column per level of
\code{by}, plus a \code{Total} column.
}
\description{
Two-way crosstab of one variable against a breakdown variable
}
