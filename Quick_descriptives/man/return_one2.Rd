% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predictor_breakdowns.R
\name{return_one2}
\alias{return_one2}
\title{One outcome summarised by one or more predictors, several statistics at once}
\usage{
return_one2(
  data,
  out,
  lev = NULL,
  recode = NULL,
  mean = FALSE,
  preds,
  pred_cols = NULL,
  user_na = TRUE,
  filter_na = "b"
)
}
\arguments{
\item{data}{A data frame.}

\item{out}{Character: name of the outcome variable.}

\item{lev}{Character vector, any of \code{"n_val"}, \code{"p_val"}, \code{"p_na"}, to
include those missingness statistics as extra rows.}

\item{recode}{Character vector of levels of \code{out} to collapse into a
binary "in this group" indicator, reported as a percentage.}

\item{mean}{Logical. If \code{TRUE}, also report the numeric mean of \code{out}
(after stripping labels). Default \code{FALSE}.}

\item{preds}{Character vector of predictor variable names.}

\item{pred_cols}{Which of \code{preds} to place in columns rather than rows:
\code{NULL} (default, uses \code{preds[1]}), a numeric index into \code{preds}, or a
character vector of predictor names.}

\item{user_na}{Logical. If \code{FALSE}, \code{labelled} user-defined missing
values in \code{preds} are kept as valid categories rather than converted to
\code{NA}. Default \code{TRUE}.}

\item{filter_na}{Which missing data to drop before summarising: \code{"b"}
(both \code{out} and \code{preds}, default), \code{"o"} (outcome only), \code{"p"}
(predictors only), or \code{"n"} (don't filter).}
}
\value{
A named list with \code{"Outcome label"}, \code{"Predictors in columns"},
\code{"Table"} (the summary tibble), and \code{"Names of preds in rows"}, in the
form expected by \code{\link[=return_one2_format]{return_one2_format()}}.
}
\description{
Cross-tabulates a single outcome variable (\code{out}) by one or more
predictors (\code{preds}), stacking together any combination of: number of
valid cases (\code{lev = "n_val"}), percentage valid (\code{"p_val"}), percentage
missing (\code{"p_na"}), percentage of cases matching a recoded set of levels
(\code{recode}), and/or the mean of \code{out} (\code{mean = TRUE}).
}
