% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crosstabs.R
\name{count_across}
\alias{count_across}
\title{Frequency counts across several variables, stacked into one table}
\usage{
count_across(
  data,
  vars = NULL,
  empty_levels = FALSE,
  labels = TRUE,
  sort = FALSE
)
}
\arguments{
\item{data}{A data frame containing the variables of interest.}

\item{vars}{Character vector of variable names to summarise. If \code{NULL}
(the default), every variable in \code{data} is used.}

\item{empty_levels}{Logical. If \code{TRUE}, unused factor levels are kept in
the output. Default \code{FALSE}.}

\item{labels}{Logical. If \code{FALSE}, variable labels are stripped from the
\code{var} column, leaving just the variable name. Default \code{TRUE}.}

\item{sort}{Logical. If \code{TRUE}, sort each variable's levels by frequency.
Default \code{FALSE}.}
}
\value{
A tibble with columns \code{var} (variable name, optionally with
label), \code{levels}, \code{n}, and \code{p} (percentage).
}
\description{
Converts each of \code{vars} to a labelled factor and counts levels, stacking
the results into a single long-format tibble (one row per variable-level
combination) rather than returning a separate table per variable.
}
