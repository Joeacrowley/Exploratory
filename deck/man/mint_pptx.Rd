% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/template.R
\name{mint_pptx}
\alias{mint_pptx}
\title{Open the Sage Mint PowerPoint template}
\usage{
mint_pptx(path = NULL)
}
\arguments{
\item{path}{Optional path to a different \code{.pptx} to use as the base deck.
Defaults to the bundled template.}
}
\value{
An \code{rpptx} object (\code{\link[officer:read_pptx]{officer::read_pptx()}}).
}
\description{
Reads the "Sage Mint Theme" template bundled with the package and returns
an \code{rpptx} object (\code{\link[officer:read_pptx]{officer::read_pptx()}}) ready for the \verb{*_slide()} builders.
}
\examples{
ppt <- mint_pptx()
ppt <- add_title_slide(ppt, "Hello", subtitle = "world")
print(ppt, target = tempfile(fileext = ".pptx"))
}
