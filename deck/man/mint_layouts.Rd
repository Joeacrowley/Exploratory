% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/template.R
\name{mint_layouts}
\alias{mint_layouts}
\title{List the usable layouts in a deck}
\usage{
mint_layouts(slides, placeholders = TRUE)
}
\arguments{
\item{slides}{An \code{rpptx} object (\code{\link[officer:read_pptx]{officer::read_pptx()}}), e.g. from \code{\link[=mint_pptx]{mint_pptx()}}.}

\item{placeholders}{If \code{TRUE} (default), return one row per placeholder
with \code{name}, \code{type}, \code{type_idx} and \code{ph_label}. If \code{FALSE}, return the
unique layout names only.}
}
\value{
A data frame, or a character vector when \code{placeholders = FALSE}.
}
\description{
A filtered view of \code{\link[officer:layout_properties]{officer::layout_properties()}} restricted to the
layouts the \verb{*_slide()} builders target. Handy when a \code{ph_with()} call
fails and you need to see the real placeholder labels.
}
\examples{
mint_layouts(mint_pptx(), placeholders = FALSE)
}
